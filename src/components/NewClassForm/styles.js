import styled from 'styled-components'

const NewClassFormWrapper = styled.div`
  width: 100%;
  max-width: 400px;
  .form-group {
    margin-bottom: 10px;
  }
  .form-buttons {
    padding-top: 10px;
    display: flex;
    justify-content: space-between;
  }
`

export default NewClassFormWrapper
